 introduction to types in javascript 
 in typescript  type annotatioon is a way of explicitely specifying the type of a variable , function parameter  , or
 function return value  .


Advantages 
1. it helps the typescript  compiler to 
enforce ttype checking and provide type safety during development . 
what operations could we performed on that variable or the value . 

type annotations are expressed by using a colon (:) followed by the desired type after the variable or unction parameter 
declaration . there can be a space after the colon . 


types in typescript 

1. undefined 
2. void 
3.any 
    1.number 
        1.number enum 
        2. never 
    2.bigint 
        1.never
    3.boolean 
        1.never
    4.string
        1.string enum
        2.never
    5.symbol
        1.unique symbol
        2.never
    6.object 
        1.array 
            1.tuple
        2.function
        3.constructor 
4. null 


home work 

1. string initialization : declre a variable message of type string and assign it the value "hello,typeScript"
2. concatenation : create two variables firstName and lastName  of type string and assign you firstName and last name , respectively .
